import { Input } from "@mui/material";

export default function RegisterPage(){
    return (
      <main className="flex min-h-screen flex-col items-center justify-between p-24 shadow-lg bg-black">
        {/* <Input placeholder="Full Name" className="w-96 h-10 bg-white rounded-3xl text-black text-right" disableUnderline={true} required={true} type="text">
        </Input> */}
      </main>
    );
}